<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/orange"
    tools:context=".activity.ConfirmationPage">

    <de.hdodenhof.circleimageview.CircleImageView
        android:layout_width="110dp"
        android:layout_height="110dp"
        android:id="@+id/id_tickMark"
        android:src="@drawable/icon_tick_mark"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="240dp"
        app:civ_border_width="7dp"
        app:civ_border_color="@color/white"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Your order has been successfully placed!"
        android:textSize="20sp"
        android:textColor="@color/white"
        android:gravity="center"
        android:layout_margin="20dp"
        android:layout_below="@+id/id_tickMark"
        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/id_buttonOK"
        android:text="OK"
        android:textSize="18sp"
        android:textColor="@color/white"
        android:padding="10dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_margin="30dp"
    />

</RelativeLayout>